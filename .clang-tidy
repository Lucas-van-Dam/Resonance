Checks: >
  -*,
  bugprone-*,
  -bugprone-easily-swappable-parameters,
  -bugprone-exception-escape

HeaderFilterRegex: '^(?!.*(vendor|Vendor)).*'

CheckOptions:
  - key: readability-identifier-naming.ClassCase
    value: CamelCase

  